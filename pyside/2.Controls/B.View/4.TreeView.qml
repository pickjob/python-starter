// 
// TreeView(QtQuick): TableView 树视图
//      property:
//          rootIndex: QModelIndex
//      signal:
//          collapsed(row, recursively)
//          expanded(row, depth)
//      method:
//          depth(row): int
//          expand(row)
//          toggleExpanded(row)
//          collapse(row)
//          collapseRecursively(row)
//          expandRecursively(row, depth)
//          expandToIndex(QModelIndex index)
//          isExpanded(row): bool